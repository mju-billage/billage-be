# 인프라 · 개발 순서 · 제외 범위

## 인프라

- dev / prod 서버 분리 (개발 중엔 EC2 공용 dev 서버 1대). DB도 분리, 공유 금지.
- Caddy = HTTPS + reverse proxy. Spring Boot는 systemd 실행.
- GitHub Actions: develop push → dev 배포 / release tag 또는 수동 → prod 배포.
- 파일은 S3(`billage-files-442908904609`, `ap-northeast-2`). 앱 설정 `billage.file.storage=S3`, dev·prod 는 `prefix` 로 구분. 로컬 개발은 `LOCAL`(디스크). DB는 주기적 덤프 → S3 백업.
- 초기에 RDS·ALB·Redis·Kafka 사용 안 함. 비밀값은 Git에 커밋 금지.
- 로컬: 앱은 IDE 실행, MySQL만 Docker Compose.

### dev 서버 구축 현황 (구현됨 — 상세·명령은 `deploy/README.md`)

- **구조**: RN 앱 →HTTPS→ Caddy(443) →reverse_proxy→ Spring Boot(systemd, 127.0.0.1:8080) → MySQL 8.4(Docker, 127.0.0.1:3306). 단일 EC2 t3.micro(Ubuntu 24.04, RAM 1GB, 스왑 2G), ap-northeast-2.
- **주소**: 탄력적 IP `52.78.148.114` 고정 → `https://52-78-148-114.nip.io` (Caddy 자동 Let's Encrypt). nip.io는 실도메인 생기면 `caddy.env`의 SITE_ADDRESS만 교체.
- **배포**: `develop` push → Actions `deploy-dev.yml`(bootJar→scp→restart). 헬스체크 실패 시 **이전 jar 자동 롤백**. 시크릿 `EC2_HOST/EC2_USER/EC2_SSH_KEY`.
- **백업**: 매일 03:30 KST mysqldump→gzip(로컬 14일) + S3 `billage-db-backup-442908904609`(SSE-S3, 30일). S3 인증은 EC2 인스턴스 역할(서버에 키 없음). 복구 `deploy/restore-db.sh`.
- **하드닝**: 앱·MySQL 외부 미노출(localhost 바인딩+보안그룹 22/80/443만), JVM Xmx384m·MySQL mem_limit 360m·performance_schema OFF, journald 200M 상한, 자가점검 cron(`healthcheck.sh`) 5분.
- **미완**: prod 환경, UptimeRobot 외부 알림 등록. (접근 제어는 auth 도메인 구현으로 해결 — `SecurityConfig` 가 `anyRequest().authenticated()`, 로그인·재발급 계열만 공개.)

## 개발 우선순위

~~1. 프로젝트 공통 설정 → 2. 예외 처리·응답 규칙 → 3. Flyway·MySQL → 4. 인증·사용자 → 5. 모임·관리자·모임원 명단 → 6. 권한 처리 → 7. 폴더 → 8. 장부(예산 포함) → 9. 수입·지출 내역 + 승인 + 수정·삭제~~ **(완료, 수정·삭제는 2026-08-17 총무 전용 확정 후 구현)**
→ ~~10. 증빙 파일~~ **(완료)** → ~~11. 대시보드·통계~~ **(완료, 2026-08-16 — 회비 집계는 Dues 이후)** → ~~12. 보고서~~ **(완료, 2026-08-17 — 권한 확정 반영)** → ~~13. 납부 관리~~ **(완료, 2026-08-19 — 마감 취소 API 는 2026-08-30 화면명세 확인으로 "만들지 않음" 확정)** → 14. 프론트 연동 QA → 15. 런칭 서버 배포

### 명세에 없는 미착수 기능 (2026-08-17 기획 문서에서 발견, 범위 확인 필요)

기획 0720 답변은 "화면명세서와 IA에 명시된 모든 기능을 구현하여 런칭"이라고 못박았는데, 다음은 **노션 API 명세에도 이 저장소에도 없다.**

- ~~**폴더 전체 백업 / 기록 보관(보관함)**~~ — 완료(2026-09-04, V18, `archive` 도메인). 보관 시 제목(20자)을 받고 모임의 현재 장부 전부를 스냅샷으로 뜬 뒤 원본 폴더·장부·내역을 지운다. 보관함은 목록·상세·제목 변경·삭제까지 있고 상세는 읽기 전용이다.
  **진행 중인 회비가 남아 있으면 거부한다**(`ARCHIVE_BLOCKED_BY_OPEN_DUES`) — 회비는 마감될 때 장부에 수입 내역을 만드는데 그 장부가 먼저 사라지면 마감할 곳이 없어진다.
- ~~**모임 관리자 내보내기**~~ — 완료(2026-08-24). 관리자 관계만 끊고 과거 내역은 유지.
- ~~**네이버 간편 로그인**~~ — **미지원 확정(2026-08-30).** 프론트 회원가입 화면의 Naver 버튼은 제거한다.
  `SocialProvider` 는 `GOOGLE`·`KAKAO` 만 유지. 되살릴 경우 `NaverTokenVerifier` 추가로 끝나며
  `social_accounts.provider` 가 `VARCHAR(20)` 이라 마이그레이션은 불필요하다.
- ~~**회비 요청**~~ — **서버 기능 아님으로 확정(2026-08-30, 화면 DUE-3-PAGE-04-0).**
  총무가 메시지를 직접 쓰고, 그 텍스트를 클립보드에 담아 OS 공유 시트로 카톡·문자에 붙여 보내는 클라이언트 기능이다.
  푸시가 아니므로 제외 범위와 충돌하지 않는다. 수신자 목록은 회비 상세의 미납부 탭 데이터를 그대로 쓴다.
- ~~**모임원 태그**~~ — 완료(2026-08-23, V12). 모임원 자유 입력 태그로 구현. 전화번호·메모도 함께 추가.
- ~~**모임 프로필(이미지) 변경**~~ — 완료(2026-08-24, V13). 생성·수정 요청에 `groupImageFileId` 적용.

### 화면명세 감사로 새로 드러난 미착수 범위 (2026-08-30)

Figma 화면명세서 ver 0.25 전 캔버스를 텍스트로 추출해 노션 API 명세·구현과 대조했다. 위 목록 외에 **화면은 있으나 명세도 구현도 없던 것**이 아래만큼 더 있었다. 상세는 각 도메인 노션 페이지 참고.

2026-08-31 에 이 중 대부분을 구현했다(PR #25\~#30). **남은 셋은 모두 결정이 먼저 필요한 것들이다** —
메일 발송 수단(회원가입·비밀번호·탈퇴), 푸시 서버 범위(알림), 백오피스 부재(고객지원).

- **회원가입 플로우 2단계 누락** — 약관 동의(필수 3종 + 마케팅), 이메일 인증(6자리·3분·재전송). 비밀번호 재설정(임시 비밀번호 메일)·비밀번호 변경·회원 탈퇴(총무 권한 이전 + 사유 수집)도 없다.
- **알림 도메인 전체** — 알림 목록·읽음·설정 토글 6종. 푸시 전제라 제외 범위(자체 푸시 서버)와 충돌하므로 런칭 포함 여부 결정이 먼저다.
- **고객지원** — 공지사항, 약관 전문, FAQ, 문의 접수. 백오피스가 없어 채우는 방법부터 정해야 한다.
- ~~**통계/분석**~~ — 완료(#30). 활성 장부, 예산 소진율 랭킹, 장부별 지출 비중(상위 4 + 기타).
- ~~**증빙자료 앨범**~~ — 완료(#30). 모임 전체 증빙을 모아 보고 원본 내역으로 역참조.
- ~~**내역 화면 전반**~~ — 완료. 모임 전체 내역 목록(장부 다중 필터 + 잔액 요약)과 내역명·장부명 통합 검색은 #26, 담당자 필드와 마감 회비 연결은 #27(V15).
- ~~**폴더 화면 전반**~~ — 완료. 폴더+장부 혼합 목록과 최상위 영역 조회는 #26, 선택 이동(다중)은 #28.
- ~~**대시보드**~~ — 완료(#30). 최근 14일 캘린더, 월간 캘린더, 마감 임박 회비 3건.
  안 읽은 알림 표시(`hasUnreadNotification`)는 필드만 열어 두고 알림 도메인이 생길 때까지 항상 false 다.
- ~~**보고서 유형 분리**~~ — 완료(#28, V16). 장부별은 기간을, 기간별은 장부를 서버가 정한다.
- ~~**회비**~~ — 완료(#25, V14). 시작일과 `SCHEDULED` 상태, 납부 상태 일괄 변경. 목록 검색은 기획 변경으로 제거.
- ~~**모임원 상세**~~ — 완료(#28). 총 납부 금액, 납부 내역, 일괄 삭제.

구현과 화면명세가 **어긋나던** 것도 하나 있었다 — 회비 마감이 미납자가 있으면 `UNPAID_MEMBER_EXISTS(409)` 로 막혔다. 한 명이라도 끝내 안 내면 그 회비를 영원히 마감할 수 없어, 총무에게 남는 복구 경로가 회비 삭제뿐이었다.
**#25 에서 고쳤다** — 미납자가 있어도 마감하고 그때까지 걷힌 금액만 수입으로 기록한다. 아무도 내지 않아 0원이면 내역을 만들지 않는다.

## 초기 범위 제외 (만들지 말 것)

마이크로서비스 · Kafka · Redis 캐시 · CQRS · 이벤트 소싱 · 커스텀 권한 · 실시간 은행 계좌 연동 · 자동 입금 내역 수집 · OCR 자동 장부 등록 · 자체 푸시 알림 서버 · 불필요한 디자인 패턴과 과도한 추상화
