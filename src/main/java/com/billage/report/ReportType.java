package com.billage.report;

/**
 * 보고서 유형. 화면명세가 생성 화면을 둘로 나누고 입력받는 항목도 다르다.
 *
 * <p>목록도 이 값으로 탭이 갈리며, 탭마다 빈 화면 문구가 다르다.
 */
public enum ReportType {

	/** 장부별 — 제목 + 장부(다중) + 구분. 기간은 담긴 내역의 실제 범위로 정해진다. */
	BY_LEDGER,

	/** 기간별 — 제목 + 기간. 그 기간에 내역이 있는 장부를 모두 담는다. */
	BY_PERIOD
}
