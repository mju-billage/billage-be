# /etc/billage/mysql.env 로 복사해서 실제 값 채우기. (절대 Git 커밋 금지)
# compose.yaml(서버용 MySQL)이 이 파일을 읽는다.
# MYSQL_PASSWORD 는 billage.env 의 DB_PASSWORD 와 동일해야 한다.

MYSQL_PASSWORD=여기에_강한_비밀번호
MYSQL_ROOT_PASSWORD=여기에_다른_강한_비밀번호
