# /etc/caddy/caddy.env 로 복사. Caddyfile 의 {$SITE_ADDRESS} 에 주입된다.
# EC2 퍼블릭 IP 의 점(.)을 하이픈(-)으로 바꿔 nip.io 를 붙인다.
# 예: IP 가 52.79.226.129 이면 -> 52-79-226-129.nip.io
# 도메인이 생기면 여기만 dev-api.내도메인.com 으로 교체.

SITE_ADDRESS=52-79-226-129.nip.io
